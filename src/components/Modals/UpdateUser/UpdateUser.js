import React from 'react';

function UpdateUser({ displayUpdateModal }) {
  return (
    <div className="modal" style={{ display: displayUpdateModal }}>
      {/* Content of the modal */}
    </div>
  );
}

export default UpdateUser;
